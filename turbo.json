{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "//#build-packages:watch": {
      "outputs": ["dist/**/*"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
